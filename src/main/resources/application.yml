spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/devdb
    username: root
    password: root
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
      application:
        name: esercizio-repositories1
        jackson:
          serialization:
            fail-on-empty-beans: false